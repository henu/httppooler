HttpPooler
==========

A tool to pool local HTTP services over an encrypted link. One server, any number of clients.


Building
--------

  ./build_release_package.sh

Constructs `httppooler.tar.gz`, that includes the tool and installation script.


Installing
----------

Extract release package and run:

  sudo ./install.sh
